<%= form_for @photo do |f| %>
  <div>
    <%= f.label :filepicker_url, "pics of project" %>
    <%= f.filepicker_field :filepicker_url %> <!-- User#filepicker_url is a regular string column -->
  </div>

  <%= f.submit %>
<% end %>


  <div>
    <%= filepicker_image_tag @photo.filepicker_url, w: 160, h: 160, fit: 'clip' %>
  </div>
